# from typing import Dict
# import re
#
# from .base import AbstractTransitionsAdapter
#
#
# STATE_NAME_PATTERN = r"[a-zA-Z_]{1}\w*"
#
#
# class ScenarioTransitionsAdapter(AbstractTransitionsAdapter):
#
#     def parse_transitions(self, content: str) -> Dict[str, Dict[str, str]]:
#
#         transitions = {}
#
#         for line in content.splitlines():
#             if line.isspace():
#                 continue
#
#             if line.startswith("[") and line.endswith("]")
#             # source_state = re.search(self._state_template, line)
#             if source_state is not None:
#                 source_state =
#

# TODO: to come up with a format for the configuration file of transitions
